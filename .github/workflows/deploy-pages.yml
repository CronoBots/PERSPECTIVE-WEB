name: Déploiement GitHub Pages

on:
  push:
    branches:
      - claude/website-perspective-conversation-4yzu4m
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurer Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Cache-busting des assets
        run: |
          VER="${GITHUB_SHA::8}"
          echo "Version des assets : $VER"
          sed -i -E "s/(href|src)=\"(styles\.css|script\.js|favicon\.svg)\"/\1=\"\2?v=$VER\"/g" index.html
          echo "Références après injection :"
          grep -E "styles\.css|script\.js|favicon\.svg" index.html

      - name: Uploader le site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Déployer
        id: deployment
        uses: actions/deploy-pages@v4
